version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/nginx
permissions:
  - object: /
    owner: ubuntu
    group: ubuntu

hooks:
  ApplicationStart:
    - location: scripts/starts.sh
      timeout: 60
      runas: ubuntu
